class CheckBit {
    static boolean checkKthBit(int num, int k) {
        int mask = 1 << k;
        int res = num & mask;

        if (res == 0) {
            return false;
        }
        return true;
    }
}
